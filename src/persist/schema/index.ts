export enum SchemaColumnType {
  ID = "BIGINT",
  BLOB = "BLOB",
  BOOLEAN = "BOOLEAN",
  DATE = "BIGINT",
  DECIMAL = "DECIMAL",
  ENUM = "ENUM",
  NUMBER = "BIGINT",
  TEXT = "TEXT"
}

export { default } from "persist/schema/shema";
